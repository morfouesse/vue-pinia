export enum ScreenType {
  LG = 'lg',
  MD = 'md',
  SM = 'sm'
}

export enum SelectedChips {
  POST_LIST = 0,
  CREATE_POST = 1,
  POSTS_HISTORY = 2
}

export enum BottomAppList {
  HOME = 'home',
  POSTS = 'posts',
  SEARCH = 'chercher'
}
